name: 'AWS Serverless Application Model Deploy'
description: 'Use AWS SAM CLI to build and deploy to Serverless Application Repository'
branding:
  icon: 'box'  
  color: 'green'

runs:
  using: 'docker'
  image: 'Dockerfile'
